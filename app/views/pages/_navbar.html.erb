  <nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
    <div class="container">
      <a href="<%= root_path %>">
        <img class="mr-2" src="/assets/logo.png" alt="" width="75" >
        <p class="navbar-brand">CarPool</p>
      </a>
      <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
        <% if @current_user.present? %>
          <%= image_tag 'http://placekitten.com/30/30', :class=>'rounded-circle' %>
          <%= @current_user.fullname %>
        <% else %>
        <i class="fas fa-bars"></i>
        <% end %>
      </button>
      <div class="collapse navbar-collapse" id="navbarResponsive">
        <ul class="navbar-nav ml-auto">
          <% unless @current_user.present? %>
          <li class="nav-item">
            <%= link_to 'Sign up', new_user_path, :class=>'nav-link js-scroll-trigger' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign in', login_path, :class=>'nav-link js-scroll-trigger' %>
          </li>
          <% end %>

          <% if @current_user.present? %>
          <li class="nav-item">
            <%= link_to "New route", new_route_path, :class=>'nav-link js-scroll-trigger' %>
          </li>
          <li class="nav-item">
            <%= link_to "All my routes", routes_my_path, :class=>'nav-link js-scroll-trigger' %>
          </li>
          <li class="nav-item">
            <%= link_to "My bookings", bookings_path, :class=>'nav-link js-scroll-trigger' %>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= image_tag 'http://placekitten.com/30/30', :class=>'rounded-circle' %>
              <%= @current_user.fullname %>
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
              <%= link_to "My Profile", @current_user, :class=>'dropdown-item' %>
              <%= link_to "Log out: #{@current_user.email}", login_path, :method=>'delete', :class=>'dropdown-item' %>
            </div>
          </li>
          <% end %>

        </ul>
    </div>
  </nav>
