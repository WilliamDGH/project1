<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
    <%= link_to 'CarPool', root_path, :class=>'navbar-brand' %>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarCollapse">
      <%= link_to 'Browse routes', routes_path, :class=>'nav-link' %>
      <div class="navbar-nav ml-auto nav-item dropdown">
        <% if @current_user.present? %>
        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
          <%= image_tag 'http://placekitten.com/30/30', :class=>'rounded-circle' %>
          <%= @current_user.fullname %>
        </a>
        <div class="dropdown-menu">

          <%= link_to "New route", new_route_path, :class=>'dropdown-item' %>
          <%= link_to "Joined trips", routes_joined_path, :class=>'dropdown-item' %>
          <%= link_to "All my routes", routes_my_path, :class=>'dropdown-item' %>
          <%= link_to "My bookings", bookings_path, :class=>'dropdown-item' %>

          <div class="dropdown-divider"></div>
          <%= link_to "My Profile", @current_user, :class=>'dropdown-item' %>
          <%= link_to "Log out: #{@current_user.email}", login_path, :method=>'delete', :class=>'dropdown-item' %>
          <% else %>
            <%= link_to 'Sign up', new_user_path, :class=>'nav-link' %>
            <%= link_to 'Sign in', login_path, :class=>'nav-link' %>
          <% end %>
        </div>
      </div>
    </div>
  </nav>
